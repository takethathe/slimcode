# 06: compaction 窗口 200k + 剩 16k 触发

**What to build:** 压缩窗口与触发语义更新。窗口默认值 128k → 200k；触发条件从"超过
92% 百分比"改为"剩余不足 16k 空 context"（估算 >= 窗口 − 16000 时压缩；200k 下即
估算 >= 184k 时压缩，< 184k 不压）。百分比常量移除；压缩后保留量（8%）不变。相关
测试（窗口常量、阈值边界）同步更新。文档中描述压缩语义的部分（用户手册、开发文档）
同步更新。

**Blocked by:** None (can start immediately) — 推荐先于 04 执行，让 footer 的
context 分母直接是 200k，避免中间态显示 128k。

**Status:** resolved

- [ ] 窗口默认值 200k。
- [ ] 触发 = 剩余 ≤16k（估算 ≥ 184k 压缩、< 184k 不压）。
- [ ] 92% 百分比常量移除；压缩后保留量 8% 不变。
- [ ] 相关测试（窗口常量、阈值边界）更新。
- [ ] 压缩语义相关文档（用户手册、开发文档）同步。
- [ ] 全量测试通过；`cargo fmt --all` 与 clippy（`-D warnings`）干净。
