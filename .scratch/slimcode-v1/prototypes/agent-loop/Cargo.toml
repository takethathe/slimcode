[package]
name = "agent-loop-proto"
version = "0.1.0"
edition = "2021"
publish = false

# PROTOTYPE for slimcode ticket 04. Throwaway — not part of the workspace.
# Explores the agent runtime loop shape against a scripted fake provider.

[workspace]

[[bin]]
name = "agent-loop-proto"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
