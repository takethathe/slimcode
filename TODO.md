# TODO

人工维护的待办输入清单：只放尚未完成的事项。维护规则见 AGENTS.md「TODO.md 仅作待办输入」。

## 待办

- [x] `AgentRunner` 运行时 seam（spec「运行时与 hooks」：loop 变结构体 + 可选闭包 hook 字段；
      ADR-0011 D1 已列出，但 arch-realignment 六张 ticket 未覆盖，见 `.scratch/arch-realignment/spec.md`）
- [ ] compact（autocompact）功能支持
- [ ] tui中status line信息显示支持
- [ ] config文件支持
- [ ] skill在query中注入方式支持
- [ ] paste 文本支持
